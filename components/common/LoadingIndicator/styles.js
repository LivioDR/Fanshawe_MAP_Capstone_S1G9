import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        justifyContent: "center",
        gap: 15,
        flex: 1,
        width: '100%',
        alignItems: 'center',
    },

    text: {
        fontSize: 18,
    },
});