import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        flexWrap: "wrap",
        columnGap: 24,
        justifyContent: "center",
    },

    clockBtn: {
        wrapper: {
            flexBasis: "45%",
        },
    },
});