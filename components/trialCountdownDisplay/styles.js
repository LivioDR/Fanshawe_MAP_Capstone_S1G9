import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20,
        gap: 25,
    },

    heading: {
        fontSize: 20,
        fontWeight: "600",
    },

    subHeading: {
        fontSize: 16,
        fontWeight: "600",
        marginTop: 6,
    },
});
