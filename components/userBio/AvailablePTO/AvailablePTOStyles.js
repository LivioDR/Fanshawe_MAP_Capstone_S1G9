import { StyleSheet } from "react-native";

export default AvailablePTOStyles = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
        width: '100%',
        paddingVertical: '5%',
    }
})