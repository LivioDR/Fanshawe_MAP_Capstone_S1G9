import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20,
        gap: 25,
    },

    settingContainer: {
        gap: 8,
    },

    heading: {
        fontSize: 20,
        fontWeight: "600",
    },

    dropdown: {
        paddingBottom: 8,
        borderBottomWidth: 1,
        borderBottomColor: "#DDD",
    },
});